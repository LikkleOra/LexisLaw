---
phase: 03-git-repair
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [".gitignore"]
autonomous: false
requirements: ["INFRA-01"]
must_haves:
  truths:
    - "git push origin master succeeds"
    - "node_modules is not in the git index"
    - ".gitignore is present and correctly configured"
  artifacts:
    - path: ".gitignore"
      provides: "Exclusion rules for git"
  key_links:
    - from: ".gitignore"
      to: "git index"
      via: "git add"
---

<objective>
Fix the git push failure caused by node_modules being accidentally committed.
This plan will create a .gitignore file, remove node_modules from the git index, 
and squash the unpushed commits into a clean state that GitHub will accept.

Purpose: Restore ability to push code to GitHub.
Output: A successful git push and a clean .gitignore.
</objective>

<execution_context>
@C:/Users/Lenovo/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .gitignore and remove node_modules from index</name>
  <files>.gitignore</files>
  <action>
    Create a comprehensive .gitignore file for a Next.js + Convex + Node project.
    Include: node_modules, .next, .env, .env.local, dist, *.log, LawOffice-main.zip, LawOffice-main/, and Screenshots.
    Then, remove node_modules from the git index without deleting files: `git rm -r --cached node_modules`.
  </action>
  <verify>ls -a .gitignore && git ls-files --cached | grep -c node_modules (should be 0)</verify>
  <done>.gitignore created and node_modules removed from git index.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite history and push</name>
  <files></files>
  <action>
    Since the previous 8 commits contain the large files, we need to reset the branch to origin/master and redo the commit cleanly.
    1. Run `git reset origin/master` (Mixed reset - keeps changes in working dir).
    2. Run `git add .` (Respects the new .gitignore).
    3. Run `git commit -m "feat: implement LawOffice application and admin features"`
    4. Run `git push origin master`
  </action>
  <verify>git status (should show branch is up to date with origin/master)</verify>
  <done>Changes pushed successfully to GitHub.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify push on GitHub</name>
  <files></files>
  <action>Verify that the push was successful and no large files are present on the remote.</action>
  <verify>Check GitHub web interface</verify>
  <done>Push verified by user</done>
  <what-built>Git history repaired and push completed.</what-built>
  <how-to-verify>
    1. Check your GitHub repository online to ensure the changes are there.
    2. Verify that node_modules is NOT in the file list on GitHub.
  </how-to-verify>
  <resume-signal>Type "approved" if push succeeded and history is clean.</resume-signal>
</task>

</tasks>

<verification>
Run `git push origin master` and verify it succeeds.
Check `git ls-files --cached node_modules` is empty.
</verification>

<success_criteria>
- git push succeeds without large file errors.
- .gitignore is present in the repository.
- No node_modules files are tracked by git.
</success_criteria>

<output>
After completion, create `.planning/phases/03-git-repair/03-01-SUMMARY.md`
</output>
